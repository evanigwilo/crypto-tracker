import { cleanup } from "@testing-library/react/";

describe("Coins Fetched", () => {

    beforeEach(async () => {
    });
    afterEach(() => {
        cleanup();
    });

    it.todo("should fetch first page");

    it.todo("should fetch second page");
});