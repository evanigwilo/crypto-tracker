import { cleanup } from "@testing-library/react";

describe("Searching for Coins", () => {
    beforeEach(async () => {
    });
    afterEach(() => {
        cleanup();
    });

    it.todo("should search and return coin");

    it.todo("should test debounce when searching");
});