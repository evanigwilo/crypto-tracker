import { cleanup } from '@testing-library/react';

describe("Coin Rows", () => {

    beforeEach(() => {
    });

    afterEach(() => {
        cleanup();
    });

    it.todo("should show header");

    it.todo("should show a coin row");

    it.todo("should show detailed graph when clicked");
});