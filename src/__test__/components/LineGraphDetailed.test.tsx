import { cleanup } from "@testing-library/react";

describe("Detailed Graph", () => {
    beforeEach(async () => {
    });
    afterEach(() => {
        cleanup();
    });

    it.todo("should show error");

    it.todo("should show graph");
});
