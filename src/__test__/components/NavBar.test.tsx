import { cleanup } from '@testing-library/react';

describe("AppBar", () => {
    beforeEach(() => {
    });
    afterEach(() => {
        cleanup();
    });

    it.todo("should contain title");
    it.todo("should contain theme toggle");
    it.todo("should contain Github icon");
    it.todo("should contain LinkedIn icon");
});

describe("AppBar Functionality", () => {

    beforeEach(() => {
    });
    afterEach(() => {
        cleanup();
    });

    it.todo("should toggle the right theme");

});