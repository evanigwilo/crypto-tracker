import { cleanup } from '@testing-library/react';

describe("Top coins by market rank", () => {
    beforeEach(() => {
    });

    afterEach(() => {
        cleanup();
    });

    it.todo("should show left coin");

    it.todo("should show center coin");

    it.todo("should show right coin");
});
